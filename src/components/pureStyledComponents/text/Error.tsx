import styled from 'styled-components'

export const Error = styled.p`
  color: ${({ theme }) => theme.colors.error};
  font-size: 1.3rem;
  font-weight: normal;
  line-height: 1.4;
  margin: 0;
  max-width: 100%;
`
