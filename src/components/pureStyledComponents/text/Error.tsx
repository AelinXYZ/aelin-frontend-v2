import styled from 'styled-components'

export const Error = styled.p`
  color: ${({ theme }) => theme.colors.error};
  font-size: 1.4rem;
  font-weight: normal;
  line-height: 1.2;
  margin: 10px auto 0;
  max-width: 100%;
  text-align: left;
`
